<?php

    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
    function _money($value)
    {
        $temp = "$" . number_format($value,"2",",",".");
        return($temp);
    }
    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
    function _number($value)
    {
        $temp = number_format($value,"2",",",".");
        return($temp);
    }
    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
    function _date($date)
    {
        $temp   = explode("-",$date);
        $temp   = $temp[2] . "/" . $temp[1] . "/" . $temp[0];
        return($temp);
    }
    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
    function _datetime($dateTime)
    {
        $date   = explode(" ",$dateTime)[0];
        $time   = explode(" ",$dateTime)[1];

        $temp   = explode("-",$date);
        $date   = $temp[2] . "/" . $temp[1] . "/" . $temp[0];

        return($date . " " . $time);
    }
    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
    function _datetimezone($dateTime)
    {
        $fecha_hora_objeto      = new DateTime($dateTime);
        $fecha_hora_formateada  = $fecha_hora_objeto->format('d/m/y H:i:s');

        return($fecha_hora_formateada);
    }
    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
    function timestamp()
    {
        return(date("Y-m-d H:i:s"));
    }
    //------------------------------------------------------------------------------------------------------------------------------------------------------------------------------//
?>